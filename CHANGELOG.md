# 更新日志

## 2.2.3

**建议所有非英文用户升级。** 共 256 个用例全部通过。

### 严重:恢复脚本会损坏 config.toml

`Restore-Native-Codex.cmd` 清理托管配置时用 `Get-Content` / `Set-Content` 且**未指定编码**。`config.toml` 是无 BOM 的 UTF-8,而 Windows PowerShell 5.1 对无 BOM 文件按**系统代码页**处理 —— 在中文等非 UTF-8 区域设置下,UTF-8 字节被当作 GBK 解码再编码写回:大部分字节侥幸原样保留,而任何不构成合法 GBK 的序列被替换为 `?`。

后果是配置被**不可逆地**破坏。实测中它毁掉了四条项目路径的最后一个中文字,并连带吃掉了 TOML 的闭合引号:

```
[projects.'d:\coding\project\自动化任务']   →   [projects.'d:\coding\project\自动化任�?]
```

文件从该行起不再是合法 TOML。**Codex 因此无法启动,并转而弹出 UAC 提权框 —— 而批准提权毫无作用,因为问题不在权限。** 这个症状极具误导性。

读写两侧现已显式指定 UTF-8。回归测试有两条:一条真实调用 PowerShell,对含中文路径的配置做往返并断言**字节完全一致**;另一条确保 `Get-Content`/`Set-Content` 那种写法不会重新出现。

若你曾运行过旧版恢复脚本,请检查 `~/.codex/config.toml` 中是否存在 `[projects.` 开头却未以 `']` 结尾的行。脚本运行前的备份可用于还原受损行。

## 2.2.2

修复应急退出路径。共 254 个用例全部通过。

### 恢复原生 Codex

- **`Restore-Native-Codex.cmd` 此前从来没能运行过。** 该文件是纯 LF 行尾,而 `cmd.exe` 要求 CRLF —— 解析器逐行吃掉开头字符(`setlocal` 变成 `ocal`,`rem` 变成 `m`),脚本 **exit 255 且什么都没做**:注册表变量全在、网关照常运行、`config.toml` 一字未动。它是为"网关或 bridge 已经坏掉"准备的最后手段,却恰恰在需要它的场合失效。已加 `.gitattributes` 并在打包时强制转换,同时加了断言防止再次发生。`Start-OpenCodex.cmd` 同样是 LF,只是内容简单侥幸能跑,一并修正。
- **恢复之后无法自动回来。** 该脚本会移除 `config.toml` 的托管块,而网关启动时只会**同步已存在**的托管块,不会重建它。于是重新运行 `OpenCodex.exe` 后:网关起来了、服务商与模型都在、界面一切正常,但 Codex 没有任何路由能到达网关,第三方模型就是不出现。唯一的出路是控制台里点一次「重启 Codex」,而用户没有理由知道这一点。现在启动时若检测到已配置第三方模型却没有托管块,会直接写回。
- **该脚本带着两个已在网关中修复、却没有同步过去的缺陷**:无条件删除 `model_catalog_json`(会删掉用户自己配置的那一行),以及按映像名 `Get-Process 'Codex','ChatGPT'` 全量强杀(会带走用户单独安装的 ChatGPT 及其未保存内容)。现在与网关一致:只删可判定属于本项目的键,只终止受管安装目录下的进程。重新拉起 Desktop 时也改为解析包族名,不再硬编码某一台机器上的值。

### 其他修复

- **删除托管的 `[model_providers.opencodex]` 段落时只删掉了标题行。** 正则在 `(?m)` 下 `$` 匹配行尾,惰性量词在第一行就停下,`name`、`base_url` 和网关令牌被留成悬空键,并被 TOML 解析器归到上一个表名下。网关与脚本两处相同缺陷一并修正。
- **`OPENCODEX_DATA_DIR` 对模型目录一直无效。** 15 处直接以 `os.homedir()` 拼出目录路径,因此 `providers.json` 认这个变量、模型目录不认 —— 网关把服务商写进一个目录,却从另一个目录读模型。全部改为统一的 `openCodexCatalogPath()`。

### 文档

- README 按当前实际情况重写,新增「恢复原生 Codex」一节,把三个出口的差异列清楚:哪些需要网关运行、哪些会清空模型选择、哪些会修复会话文件。此前这些差异只散落在按钮提示与脚本注释里。

## 2.2.1

打包体积与测试质量,无功能变更。共 247 个用例全部通过。

### 打包

- **发行包 2.63 MB → 1.05 MB。** 两个 logo 各约 865 KB、尺寸 1254×1254,而样式表把它们显示为 **42×42** —— 1.69 MB 占了整个下载的六成以上,只为一个界面图标。缩至 256×256(显示尺寸的 6 倍余量)后共 88 KB,透明通道保留;原图留在 git 历史里。其中 `opencodex-logo.png` 页面从来不请求,只有网关路由引用它。
- 新增断言:页面引用的任何图片超过 200 KB 即失败。上限给得很宽,只拦"原图未缩放就贴进来"这一种情况 —— 它已经悄悄发生过一次。

### 测试

把几条用正则读源码的测试改成验证真实行为。这类测试在本轮里三次假红(端口竞争、文件权限、重启顺序),并且漏掉了一个真 bug(仪表盘语法错误 —— 金标准测试查了标签配对和 id 齐全,全部通过)。

- **网关只监听回环 + `/api` 鉴权**:原先是四条正则匹配 `gateway.ts` 里的监听地址与 `requireAdmin` 调用。现在真起一个网关:断言 `127.0.0.1` 可连、**本机每一个非回环 IPv4 都拒绝连接**、`/api/providers` 无凭据 401、带正确令牌 200、带等长错误令牌 401、带浏览器 cookie 200。
- **仪表盘的 5 条契约测试**:原先拼接 5 个 TypeScript 模块再 grep。现在直接断言 `getDashboardHtml()` 的输出 —— 若某个模块从组装里掉了,读源码的写法会照常通过而功能已经从页面消失。

有两处**故意保留**为源码检查,并在代码里注明了原因:令牌比较是否恒定时间(黑盒观测不到,只能断言机制);以及"没有文件手写 `os.homedir()/.codex`"(它防的是**将来新增**的错误写法,没有任何行为测试能观察到)。

## 2.2.0

一次完整安全审计后的修复版本。所有改动都有对应测试,当前共 245 个用例全部通过。

### 升级前必读

- **最低 Node 版本提升到 22.19**。之前 README 写的是 20+,但锁定的 `undici@8.5.0` 声明 `engines.node >= 22.19.0` —— 按旧文档装 Node 20 会在安装或运行时失败。
- **`deepseek-v4-pro` 已从 DeepSeek 默认预设移除**。DeepSeek 服务端目前拒绝该模型用于 Codex 集成;等官方开放后手动添加即可。已配置的用户不受影响。
- **子代理不再自己执行命令**。`exec_command` 现在返回提示,要求把命令交还主 Agent 通过 Codex 的审批与沙箱机制运行(详见下方 P1-5)。
- **`OPENCODEX_AGENT_MESSAGE_ORACLE` 保持默认开启**。若你从旧版复制了自己的 `opencodex.env`,请确认这一行没有被注释掉,否则第三方子代理会以 502 `unreadable_encrypted_agent_task` 失败。
- **升级后请同时重启 Codex Desktop**,只重启网关会留下旧的 bridge。

### 安全

- **上游入口需要凭令牌访问**(P1-4)。此前只有 `/api/*` 有鉴权,而 `/v1/responses`、`/responses/compact`、`/v1/images/generations` 是敞开的,且网关会把缺失或占位的 bearer 替换成 `auth.json` 里真实的 ChatGPT access token。任意网页都能用 `Content-Type: text/plain`(简单请求,不触发预检)向 127.0.0.1 发起请求,并以用户身份执行。CORS 会挡住读取响应,所以这是盲发请求伪造而非 token 泄露 —— 但请求确实执行了,消耗用户配额。绑定回环地址不构成防护。

  判定依据是**是否持有网关令牌**,而不是请求头像不像浏览器:网页拿不到该令牌,也无法在简单请求上设置 `Authorization`(会触发预检)。bridge 在把原生出口请求转发给网关时会附带该令牌,并在这一跳剥掉 `Origin`/`Referer`/`Sec-Fetch-*` —— 把调用方的浏览器请求头转发到另一个 origin 本就没有意义。浏览器特征仅作为无令牌时的兜底。
- **移除子代理的 shell 执行**(P1-5)。原实现无条件调用 `/bin/zsh -lc`,在 Windows 上必然失败。改成 cmd/PowerShell 会把一个坏掉的功能变成能用的漏洞:工作目录之外没有任何约束,完全绕开 Codex 的审批与沙箱链路。

### 修复

- **原生出口开关从未生效**(P0-1)。`OPENCODEX_NATIVE_EGRESS` 是 2.1.0 的主打功能,两层缺陷叠加导致它一次都没工作过 —— 生产者读取自己写入 `process.env` 的输出,第一次设为 `1` 后就永久锁死。
- **测试套件会破坏本机环境**(P0-2)。构造 `CodexBridgeServer` 会向真实的 `HKCU\Environment` 写入 `CODEX_CLI_PATH`,停止时删除全部六个 bridge 变量,把正常工作的 Codex Desktop 从网关上摘下来。
- **删除的模型会复活**(P1-1)。目录过滤条件写反了:删掉的是仍被选中的模型,保留的是刚被移除的。
- **重置会删除用户自己的 Codex 配置**(P1-2)。
- **会话修复越界**(P1-3)。
- **连接测试谎报成功**(P1-6)。此前只有 401/403 算失败,404(Base URL 写错)、429(限流)、5xx(上游故障)全部报告"连接成功"。
- **保存失败仍返回 200**(P1-7)。`saveProviders()` 吞掉写入错误,只打日志;只读目录或杀软占用会导致界面显示"已保存"而重启后配置为空,且 API Key 已进入系统凭据库,两边不一致。
- **重启会强杀不属于本程序的进程**(P1-8)。原实现按映像名 `taskkill /F /T /IM`,包括 `ChatGPT.exe` —— 用户单独安装的 ChatGPT、另一个 Codex 窗口、其中未保存的工作,都会一并丢失。现在按可执行文件路径判定归属。
- **硬编码模型版本**(P1-10)。"恢复原生"会把用户的 `model` 行改写成固定的 `gpt-5.5`。
- **`CODEX_HOME` 未被尊重**(P2-1)。11 处直接拼 `os.homedir()/.codex`,其中 3 处是模块加载时常量,自定义 Codex 目录的用户无法通过设置环境变量修正。
- **响应缓冲无上限**(P2-2)、**关键文件写入非原子**(P2-3)。
- **打包默认值与自身文档矛盾**(P2-4)。生成的 `opencodex.env` 写入 `OPENCODEX_AGENT_MESSAGE_ORACLE=1`,而同一脚本生成的 README 说它默认关闭。**修正的是文档,不是默认值** —— 关掉它第三方子代理会拿到空任务并直接失败,也就是这个 fork 的核心能力完全不可用。现在两边都写明:默认开启,代价是每个不同子代理任务多一次 ChatGPT 请求;不想付这个代价就自行注释掉。

### Windows 平台(审计外发现)

- **截图优化在 Windows 上从未生效**。唯一的缩放实现是 macOS 的 sips,守卫使其在其他平台返回 `null`,而调用方把 `null` 当作"保留原图" —— 所以它看起来是正常的。实际每张超限截图都以原分辨率发给服务商。现已提供 System.Drawing 实现。
- **网关重启路径硬编码 Homebrew 路径**。`/opt/homebrew/bin/pm2` 在 Intel Mac 上也是错的,Windows 上必然抛异常并被静默接管,但响应仍告诉用户"网关服务正在重新启动"。
- **日志面板在 Windows 上永远为空**,此前只读 `~/.pm2/logs`。

### 控制中心

- **仪表盘整个脚本此前无法运行。** `app.ts` 里"脱离 OpenCodex"确认框的字符串跨了行;整个文件是 TS 模板字面量,换行在其中合法,所以 tsc 编译通过、测试全绿、金标准文件也对得上 —— 但吐出的 JS 是 `Uncaught SyntaxError`。一个语法错误让脚本整体死掉,所有接口请求都没发出,页面永远停在"正在读取接入模板…"。
- **图标在便携包里一直是 404。** logo 只存在于 `src_v2/assets/`,构建与打包都没复制它,而网关按 `process.cwd()/src_v2/assets` 查找 —— 发行包里没有 `src_v2/`。现在构建把它放进 `dist/assets`,网关按模块自身位置解析,不再依赖工作目录。
- 新增 `dashboard_script.test.mjs`:用 `vm.Script` 解析每个内联脚本(等价于浏览器执行前的解析),并逐个请求页面引用的图片断言可用。此前 245 个测试没有一个解析过**产出的 JavaScript**,只验证过生成它的 TypeScript。

### 文档

- README 补齐端口行为:占用者是**另一个 OpenCodex 实例**时一律拒绝启动、不顺延(两个实例会争抢注册表与 Codex 配置)。仅当占用者是别的程序时才顺延。
- 说明发行包**不需要系统级 Node**:启动器先找包内 `node.exe`,再回落到 Codex Desktop 自带的运行时。

### 打包

- 移除包内重复的 `dist/OpenCodex.exe`(与根目录字节相同,271 KB,无任何引用)与 `package-lock.json`。包体 3.16 MB → 2.90 MB。
